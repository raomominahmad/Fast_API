[project]
name = "fast_api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.140.0",
]
# run script for fast api server
[tool.fastapi]
entrypoint = "main:app"

[dependency-groups]
dev = [
    "black>=26.5.1",
    "ruff>=0.16.0",
]
